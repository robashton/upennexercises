module Golf where

--skips :: [a] -> [[a]]
--everyNths :: [a]
--
--skips :: [a] -> [[a]]
----skips [] = []
----skips y@(_:r) = [y] ++ skips r
--skips xs = map (\x ->
--
--
--
--localMaxima :: [Integer] -> [Integer]
--localMaxima [x,y,z]
--  | x < y && y > z = [y]
--  | otherwise = []
--localMaxima (x:xs@(y:(z:_)))
--  | x < y && y > z = y : localMaxima xs
--  | otherwise = localMaxima xs
--localMaxima _ = []

-- Sod this, I was going to do them all long form and then find appropriate haskell functions but then I realised that this is something I'll never actually finihs..
